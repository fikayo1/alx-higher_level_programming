From the 1-my-list Module
========================

Using ``MyList``
-------------

Importing the function (Mylist) from the module:
        >>> MyList = __import__("1-my_list").MyList

checking module doc string:
        >>> m = __import__("1-my_list").__doc__
        >>> len(m) > 1
        True

checking class docstring:
        >>> w = __import__("1-my_list").MyList.__doc__
        >>> len(w) > 1
        True

cheking method docstring:
        >>> w = __import__("1-my_list").MyList.print_sorted.__doc__
        >>> len(w) > 1
        True

checking proper inheritance of list:
        >>> issubclass(MyList, list)
        True

checking the method works properly with an empty list:
        >>> l = MyList()
        >>> l.print_sorted()
        []

checking that you can append to the list:
        >>> l = My_list()
        >>> l.append(3)
        >>> l.append(6)
        >>> l.append(8)
        >>> l.append(18)
        >>> l.append(122)
        >>> print(l)
        [3, 6, 8, 18, 122]

check for a list already in order:
        >>> l.print_sorted()
        [3, 6, 8, 18, 122]

checking that you can append to the list and it gets sorted:
        >>> l = My_list()
        >>> l.append(122)
        >>> l.append(6)
        >>> l.append(8)
        >>> l.append(18)
        >>> l.append(3)
        >>> l.print_sorted()
        [3, 6, 8, 18, 122]

check for negative numbers with positive:
        >>> l = My_list()
        >>> l.append(122)
        >>> l.append(18)
        >>> l.append(-1)
        >>> l.append(-10)
        >>> l.append(3)
        >>> l.print_sorted()
        [-10, -1, 3, 18, 122]

checking for negative numbers:
        >>> l = My_list()
        >>> l.append(-122)
        >>> l.append(-6)
        >>> l.append(-8)
        >>> l.append(-18)
        >>> l.append(-2)
        >>> l.print_sorted()
        [-122, -18, -8, -6, -2]

checking for correct number of arguments:
        >>> l = My_list()
        >>> l.append(122)
        >>> l.append(18)
        >>> l.print_sorted(1)
        Traceback (most recent call last):
        ...
        TypeError: print_sorted() takes 1 positional argument but 2 were given
        
